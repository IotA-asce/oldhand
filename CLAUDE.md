# Claude Code Adapter

The canonical workspace instructions are in `AGENTS.md`.

Read and follow `AGENTS.md` before substantial work. Use the canonical policies under
`agent/` and any more-specific repository-level `AGENTS.md`.

Claude-specific skills, rules, hooks, and subagents may automate or accelerate the
workflow, but they do not override or replace canonical workspace policy.

Do not store engineering requirements exclusively under `.claude/`.
