# Ten lifecycle improvements

The executable plan is maintained at
`docs/plans/2026-09-18-ten-lifecycle-improvements.md`.

This is a standard feature task. It adds read-only discovery and safe canonical
record mutations without changing Lore's schema, scoring model, or storage
architecture. Acceptance requires ten separately tested and committed
iterations, a complete visual README rewrite, a passing clean-checkout suite,
and successful merge and push.
