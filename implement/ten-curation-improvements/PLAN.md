# Ten curation improvements

The executable plan is maintained at
`docs/plans/2026-09-18-ten-curation-improvements.md`.

This standard feature task adds archive initialization, deterministic creation,
curation commands, relationship inspection, safe id renames, prepared-target
compaction, JSON validation, and a complete visual README rewrite. It preserves
the record and index schemas and does not change ranking behavior.
